class Reply{
  final commentId;
  final replyId;
  final content;
  final openId;
  final nick;
  final createTime;

  Reply(this.commentId, this.replyId, this.content, this.openId, this.nick, this.createTime);
}